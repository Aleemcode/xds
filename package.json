{
  "name": "xds",
  "private": true,
  "version": "1.0.0",
  "description": "xDS — Xpert Design System. AFEX's design system: tokens, components, documentation.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "tokens": "npm run tokens -w @afex/xds-tokens",
    "audit": "npm run audit -w @afex/xds-tokens",
    "dev": "npm run dev -w @afex/xds-docs",
    "build": "npm run tokens && npm run audit -- --quiet && npm run build -w @afex/xds-docs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
